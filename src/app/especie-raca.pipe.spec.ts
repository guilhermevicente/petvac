import { EspecieRacaPipe } from './especie-raca.pipe';

describe('EspecieRacaPipe', () => {
  it('create an instance', () => {
    const pipe = new EspecieRacaPipe();
    expect(pipe).toBeTruthy();
  });
});
